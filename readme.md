1️⃣ What is the difference between var, let, and const?
<br>
Ans:<br>
Var:->Can re-declare
<br>->can update
<br>
let:->Cannot re-declare
<br>->can update
<br>
const:->Cannot re-declare
<br>->cannot update
<br>
<br>
2️⃣ What is the spread operator (...)?
<br>Ans:It is used to copy and extend arrays or object.
<br>
<br>
3️⃣ What is the difference between map(), filter(), and forEach()?
<br>Ans:
<br>
map():->Returns a new array
<br>->used to change data
<br>filer():->Returns a new array
<br>->used to select data
<br>forEach():->Does not return anything
<br>->Just loops and dose work
<br>
<br>
4️⃣ What is an arrow function?
<br>Ans:An arrow function is short way to  write function using =>.
<br>It makes code cleaner and doesn't use the function keyword.
<br>
<br>
5️⃣ What are template literals?
<br>Ans:Template literals are string written using backticks (``).
<br>They allow you to insert variable easily using ${} and support multiple line text.
<br>
<br>
